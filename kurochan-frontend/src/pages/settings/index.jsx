import React from 'react';
import ProfileSettings from './ProfileSettings';

/**
 * Componente principal para a página de configurações
 * Este componente serve como ponto de entrada para o módulo de configurações
 */
const Settings = () => {
  return <ProfileSettings />;
};

export default Settings;