import React from 'react';
import RelatoriosList from './RelatoriosList';

/**
 * Componente principal para a página de relatórios
 * Este componente serve como ponto de entrada para o módulo de relatórios
 */
const Relatorios = () => {
  return <RelatoriosList />;
};

export default Relatorios;