import React from 'react';
import UsersList from './UsersList';

/**
 * Componente principal para a página de usuários
 * Este componente serve como ponto de entrada para o módulo de usuários
 */
const Users = () => {
  return <UsersList />;
};

export default Users;